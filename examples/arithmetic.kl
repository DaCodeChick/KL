{Test basic arithmetic and variables}

Module Main;

Command Main;
	var x, sint32, 10;
	var y, sint32, 20;
	var result, sint32;
	set result, Add[x, y];
	
	{Result should be 30}
EndCommand;

EndModule;
