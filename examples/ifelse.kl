{Test if/else control flow}

Module Main;

Command Main;
	var x, sint32, 15;
	
	If Equal[x, 10];
		var msg1, text, "x is 10";
	ElseIf Equal[x, 15];
		var msg2, text, "x is 15";
	Else;
		var msg3, text, "x is something else";
	EndIf;
EndCommand;

EndModule;
